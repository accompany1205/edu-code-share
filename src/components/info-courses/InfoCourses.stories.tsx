import type { Meta } from "@storybook/react";

import { InfoCourses } from "./index";

const Component: Meta<typeof InfoCourses> = {
  component: InfoCourses,
  title: "InfoCourses",
};

export default Component;

export const Default = {
  args: {},
};
